# Godot editor cache — regenerated by the editor, not source
.godot/

# Godot export configuration / credentials
export_presets.cfg

# Export output
build/

# Vendored codebase-memory-mcp
codebase-memory-mcp/

# Generated translation files
translations/*.translation

# Other
*.csv.import
